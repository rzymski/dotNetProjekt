@page
@model StyleShareWebsite.Pages.AdminPanel.StylePacks.EditModel

@{
    ViewData["PageName"] = ViewData["Title"] = "Panel administracyjny";
}

<h1>Edit</h1>

<h4>StylePack</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="StylePack.Id" />
            <div class="form-group">
                <label asp-for="StylePack.Name" class="control-label"></label>
                <input asp-for="StylePack.Name" class="form-control" />
                <span asp-validation-for="StylePack.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
