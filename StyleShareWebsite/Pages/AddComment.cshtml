@page
@model StyleShareWebsite.Pages.AddCommentModel
@{
    ViewData["PageName"] = ViewData["Title"] = "Dadawanie komentarza";
}
<h4>Comment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="comment.Title" class="control-label"></label>
                <input asp-for="comment.Title" class="form-control" />
                <span asp-validation-for="comment.Title" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="comment.PostableId" value="@Model.PostableId"/>
            @*<div class="form-group">
                <label asp-for="comment.PostableId" class="control-label"></label>
                <select asp-for="comment.PostableId" class ="form-control" asp-items="ViewBag.PostableId"></select>
            </div>*@
            <div class="form-group">
                <label asp-for="comment.Content" class="control-label"></label>
                <input asp-for="comment.Content" class="form-control" />
                <span asp-validation-for="comment.Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<a href='javascript:history.go(-1)'>Back</a>
